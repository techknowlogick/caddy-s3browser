{
	"^([0-9]+\\.[0-9]+\\.[0-9]+)$": {
		"default": {
			"icon": "release",
			"description": "Previous Release $1"
		},
		"latest": {
			"description": "Current Release $1",
			"icon": "current"
		}
	},
	"^([0-9]+\\.[0-9]+\\.[0-9]+)-(rc[0-9]+)$": {
		"default": {
			"icon": "older",
			"description": "Release Candidate $2 for $1"
		},
		"latest": {
			"description": "Release Candidate $2 for $1",
			"icon": "working"
		}
	},
	"^([0-9]+\\.[0-9]+\\.[0-9]+)-dev$": {
		"default": {
			"icon": "working",
			"description": "Development branch for $1"
		}
	},
	"^([0-9]+\\.[0-9]+)$": {
		"default": {
			"icon": "older",
			"description": "Maintenance branch for $1"
		},
		"latest": {
			"description": "Development branch for $1",
			"icon": "working"
		}
	},
	"^([0-9]+)$": {
		"default": {
			"icon": "older",
			"description": "Maintenance branch for $1"
		},
		"latest": {
			"description": "Development branch for $1",
			"icon": "working"
		}
	},
	"^master$": {
		"default": {
			"icon": "working",
			"description": "Head development branch"
		}
	},
	"folder": {
		"default": {
			"icon": "folder"
		}
	},
	"file": {
		"default": {
			"icon": "file"
		}
	}
}